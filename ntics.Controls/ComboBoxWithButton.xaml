<UserControl x:Class="ntics.Controls.ComboBoxWithButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ntics.Controls"
             Name="comboxWithButton"
             mc:Ignorable="d" 
             d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ComboBox HorizontalAlignment="Stretch" Grid.Column="0" ItemsSource="{Binding ElementName=comboxWithButton, Path=ItemsSource}" SelectedItem="{Binding ElementName=comboxWithButton,Path=SelectedItem}" ></ComboBox>
        <Button Click="Button_Click"  HorizontalAlignment="Right" Grid.Column="1">...</Button>
    </Grid>
</UserControl>
